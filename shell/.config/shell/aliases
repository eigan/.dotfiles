# Linux
alias halt="shutdown -h now"
alias restart="shutdown -r now"
alias df="df -x tmpfs -x devtmpfs -h"
alias c="clear"

# Package management
alias archupdate="aurman -Syu"
alias archinstall="aurman -S"
alias archsearch="aurman -Ss"
alias archquery="aurman -Qi"
alias archremove="aurman -R"

# xorg
alias x="startx"

# Homestead
alias hup="cd $HOMESTEAD && vagrant up"
alias h="cd $HOMESTEAD && vagrant ssh"

# PHP
alias phpx='php -d "zend_extension=xdebug.so"'
alias phpxu='phpx vendor/bin/phpunit'

# Docker / xdebug thing
alias wx="sh docker/xdebug_toggle"

# Laravel
alias a="php artisan"

# Docker utils
alias wx="./docker/xdebug_toggle"

# PHP
alias phpx="php -d zend_extension=xdebug.so"
alias phpxu="php -d zend_extension=xdebug.so vendor/bin/phpunit"
alias phpp="php -d zend_extension=xdebug.so -d xdebug.profiler_enable=1"
